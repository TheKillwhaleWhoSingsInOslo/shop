# FIM代码补全能力测评平台项目策划方案

## TSP启动会议纪要（第一至第七次会议）

本报告基于团队软件过程（TSP）启动会议的九步法，系统记录了前七次会议的核心内容。项目围绕“大语言模型FIM代码补全能力测评系统”展开，依次完成了战略对齐、战术规划与风险控制三个阶段。以下按会议顺序详尽呈现各项讨论、决策与输出。

---

## 第一阶段：战略对齐

### 会议一：建立产品目标和业务目标

**会议目标**  
理解高层意愿与项目愿景，明确“为什么做”和“做什么”。将抽象的战略意图转化为可量化的业务目标与产品目标，形成全员共识的产品目标声明书。

**会议前置准备**  
- 管理层（助教/课程负责人）已提前分发《FIM测评平台需求文档》及现有实现基线说明。  
- 团队成员预习材料，列出对项目背景、痛点、约束的疑问清单。

---

#### 1. 核心讨论问题与回答

**问题1：为什么要开发这个测评平台？当前最大的痛点是什么？**  
- 管理层阐述：现有课程项目中学生模型评测完全依赖助教手工操作（下载权重、检查目录、运行脚本、拼接报告）。  
  - 痛点1：低效易错——整班提交（>100人）手工处理需数天，容易遗漏或错判。  
  - 痛点2：结果漂移——不同助教、不同时间、不同环境运行结果不一致，影响公平性。  
  - 痛点3：黑盒不透明——学生无法及时了解评测状态和失败原因；助教难以定位问题（模型错误？数据错误？环境错误？）。  
  - 痛点4：缺乏审计——无法复现历史评测结果，无法追溯榜单生成过程。  
- 团队确认：理解痛点，明确系统必须解决“自动化、标准化、可审计”三个核心诉求。

**问题2：项目的商业/教学价值具体体现在哪些方面？**  
- 管理层要求区分有形与无形效益。  
- 团队讨论后归纳：  
  - **有形效益**：  
    - 助教处理整班提交时间从“天级/手工”降至“小时级/自动化”，人力成本降低80%以上。  
    - 学生提交模型后2小时内获得初步评测反馈，加速模型迭代闭环。  
    - 通过任务队列和增量下载，有效管理华为云NPU资源，避免空转浪费。  
  - **无形效益**：  
    - 公平性与公信力：同一规则下所有学生排名，提升课程公信力。  
    - 可审计性与可复现性：保存评测配置、代码版本、日志和结果，任何榜单可回溯。  
    - 技术品牌与影响力：可作为学院成果展示或开源，提升技术声誉。  
    - 教学体验：透明化评测流程和详细错误报告，帮助学生快速定位问题。

**问题3：业务目标应设定为哪些可量化指标？**  
- 管理层要求使用SMART原则。  
- 团队提出并经批准的目标：  
  - **目标1（效率）**：2周内上线核心功能，支撑一次不少于50人次的模拟提交全流程评测，总耗时（从拉取到榜单发布）不超过4小时。  
  - **目标2（公平）**：同一批学生提交，在相同配置下任意两次评测运行的核心排名指标（Score、ES）差异不超过0.5%。  
  - **目标3（可审计）**：对于任何一个已发布榜单，系统必须能完整回溯生成该榜单所用的评测配置、后端版本、评分代码快照和原始中间结果（generation JSONL）。  
  - **目标4（可用性）**：在学生提交高峰期（预计作业截止前6小时），系统能稳定处理并发评测任务，核心API可用性达99.9%。

**问题4：产品范围边界是什么？第一期必须包含哪些功能？哪些明确不包含？**  
- 团队参照需求文档FR列表，提出MVP范围：  
  - **包含（第一期）**：  
    - FR-01+FR-02：提交拉取与根目录校验（集成`ta-client`，支持OBS临时凭证）。  
    - FR-04+FR-05：核心评测链路，优先MindSpeed-LLM后端，实现prompt导出→批量生成→结果恢复→指标计算。  
    - FR-03+FR-06/FR-07：支持至少2个核心FIM测试集（如HumanEval-FiM, RepoEval-FiM），生成内部聚合报告和外部公开榜单（脱敏）。  
    - FR-08+FR-09：状态管理（7种状态）与审计元数据保存。  
    - FR-11：实时日志可视化（助教端Web实时查看进度和日志）。  
  - **不包含（明确排除）**：  
    - 复杂服务化接口（RESTful API供外部调用）——第一期仅保证命令行+简单Web UI。  
    - 学生自主触发评测——学生只能查看本人已发布结果。  
    - 多后端动态调度与资源池——后端选择在创建任务时确定，不实现自动切换。  
    - 分布式/增量评测——所有任务在一个Worker（助教Notebook）上顺序或简单并行执行。  
    - 复杂的镜像管理——推理后端环境预先配置在Notebook中，不作为动态容器调度。  
    - 长期权重缓存策略——作为二期规划。

**问题5：如何衡量项目成功？验收标准是什么？**  
- 团队分三类提出：  
  - **功能验收**：  
    - 能成功拉取包含5名学生提交（含错误格式）的测试集，准确标记目录结构错误。  
    - 能成功触发对一个正确提交的学生模型（或基准模型）的MindSpeed评测任务，正确生成generation JSONL。  
    - 能根据生成结果正确计算出EM、ES、tle_ratio、复读率等指标，并与手工验证一致。  
    - 生成的公开榜单HTML不包含任何本地路径、服务器用户名或原始输入样本。  
    - 助教Web页面能实时刷新显示正在进行的评测任务的日志进度。  
  - **非功能验收**：  
    - 可靠性：模拟OBS瞬断或Worker崩溃，任务能重试后完成或明确失败，不产生脏数据。  
    - 安全性：系统配置文件中不包含任何长期AK/SK，所有OBS访问均通过临时凭证。  
    - 公平性：固定输入和配置下，三次运行同一学生模型的最终Score差异不超过1%。  
    - 性能：对一个7B参数量模型，单卡生成1000条补全的平均延迟不超过2秒/条。  
  - **项目交付验收**：  
    - 提交完整的《项目策划方案》（含全部9个会议记录）。  
    - 提供包含核心代码、配置示例和助教使用文档的代码仓库。  
    - 提供可部署到当前华为云助教Notebook环境的一键部署脚本或详细步骤说明。

---

#### 2. 会议输出

- **《FIM测评平台V1.0产品目标声明书》** 正式版本（内容基于上述讨论，包含业务目标、产品目标、范围边界、成功标准）。  
- **待澄清问题清单**：无，所有问题当场澄清。  
- **待办**：产品方代表（助教）在2小时内将声明书邮件发送给全体团队成员并签字确认。

---

### 会议二：角色分配和小组目标定义

**会议目标**  
将整体目标分解为具体职责，进行角色分配，明确“谁来做”。建立团队协作规则，并设定3天冲刺的里程碑。

**会议前置**  
- 会议一的产品目标声明书已签字确认。  
- 项目组长根据团队成员技能草拟了角色候选名单。

---

#### 1. 核心讨论问题与回答

**问题1：团队只有4人，如何划分角色才能覆盖所有关键职责？**  
- 教练引导：采用“核心模块Owner制”，每人负责一个纵向领域，同时交叉评审。  
- 讨论后确定四个角色及其职责：  

| 角色                 | 核心职责（基于需求文档）                                     | 关键交付物（3天内）                                          |
| :------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **项目组长**         | 全局把控与协调；整体架构设计（模块边界、API契约）；统筹代码集成；确保2周上线时间约束；与助教沟通确认需求优先级。 | 系统架构图与技术选型报告；集成后的主干代码库；《项目策划方案》整体统稿。 |
| **后端与评测经理**   | 核心评测逻辑与多后端适配；实现评测执行引擎（Evaluator）；对接MindSpeed-LLM（昇腾NPU），实现批量生成、结果恢复；计算EM、ES、TLE、复读率等指标；支持从已有raw结果重算。 | 可运行的评测脚本/服务（命令行版本），至少跑通MindSpeed一个测试集；指标计算Python包，输出与需求一致的结果JSON；提供后端适配器接口文档。 |
| **数据流与安全经理** | OBS交互与安全凭证；开发`ta-client`模块，使用STS临时凭证拉取学生提交；实现模型根目录校验（`config.json`、`tokenizer.json`、权重文件）；确保Secret管理（不存储长期AK/SK）、数据隔离（NFR-01/02）。 | 稳定的OBS拉取与校验脚本，可模拟列表、下载、校验全流程；安全凭证管理方案（环境变量/配置文件示例）；生成的公开榜单过滤函数（移除路径、逐样本内容）。 |
| **前端与运维经理**   | Web控制台与部署；设计助教端Web UI（任务创建、日志实时查看）；生成公开榜单HTML；负责Docker容器化打包与华为云Model Arts部署脚本；实现实时日志长连接或轮询方案。 | 可交互的Web UI原型（至少展示任务列表、日志流面板）；Docker镜像及启动脚本，能在助教Notebook环境一键启动；榜单HTML模板（脱敏版）。 |

**问题2：如何设定3天内可检验的小组目标？**  
- 基于“6月5日前完成方案+可演示原型”的总要求，设定三个里程碑：  

| 里程碑                 | 截止时间   | 目标描述                                                     | 产出物                                                       | 责任人                 |
| :--------------------- | :--------- | :----------------------------------------------------------- | :----------------------------------------------------------- | :--------------------- |
| **M1：方案设计冻结**   | 第1天18:00 | 确定所有接口契约、OBS目录结构、API定义、数据库/文件存储设计。 | 接口文档（含请求/响应示例）；存储设计（评测任务元数据、日志路径）；技术选型确认书。 | 项目组长（全体协助）   |
| **M2：核心模块原型**   | 第2天18:00 | 各成员跑通负责模块的“Hello World”版本：后端成功调用MindSpeed生成一条补全；数据成功从OBS拉取一个学生提交；前端画出Web控制台静态页面。 | 后端评测脚本及日志；数据拉取脚本及校验报告；前端HTML/CSS原型。 | 各角色分头交付         |
| **M3：集成与文档交付** | 第3天18:00 | 将所有模块拼装成端到端流程（至少能触发一个评测任务，看到日志和结果），完成《项目策划方案》最终版。 | 可运行的Git仓库（含README部署步骤）；《项目策划方案》PDF（含TSP全部9个会议记录）；端到端演示录屏（5分钟）。 | 项目组长（集成）、全体 |

**问题3：针对华为云环境依赖、指标计算复用、榜单脱敏等关键事项，应达成哪些共识？**  
- 团队讨论并逐项决策：  
  - **华为云环境**：不要求每人申请账号。运维经理负责在助教Notebook建立虚拟环境，其他人使用本地模拟模式（mock OBS、mock NPU）。  
  - **指标计算**：完全复用现有代码仓库中的`results_evaluator`和公共指标脚本。后端经理负责封装为稳定API，不修改核心算法。  
  - **榜单脱敏**：数据经理实现`filter_public()`函数，移除所有本地绝对路径、模型路径、逐样本内容；前端经理使用过滤后的数据渲染HTML。  
  - **实时日志**：首期采用轮询（每2秒请求一次进度API），降低复杂度。后端经理定期写入`progress.json`和`tail.log`。

**问题4：团队协作规则如何制定？**  
- 全体同意以下规则：  
  - **每日站立会**：每天固定时间（15分钟），同步昨日完成、今日计划、阻塞问题。  
  - **代码管理**：Git仓库，分支策略`main`（稳定）、`develop`（集成）、`feature/角色_功能`；所有合并需至少一人Code Review。  
  - **文档标准**：所有设计决策、接口变更记录在共享Notebook（如腾讯文档/飞书）。  
  - **沟通工具**：即时消息群，重要通知@所有人。紧急问题可随时电话。  
  - **验收标准**：每个里程碑产出由项目组长确认，出现偏差当天加急修正。

---

#### 2. 会议输出

- 角色分配正式生效，每位成员明确自己的职责与关键交付物。  
- 3天里程碑计划（M1、M2、M3）通过。  
- 关键决策（环境、指标、脱敏、日志）记录在案。  
- 待办行动项：  
  - 项目组长：整理架构图草稿，分配M1接口文档编写任务。  
  - 后端经理：调研现有evaluator，提供命令行调用示例。  
  - 数据经理：编写本地模拟环境指南，设计OBS目录校验逻辑。  
  - 运维经理：绘制Web UI线框图，准备Docker基础镜像。

---

## 第二阶段：战术规划

### 会议三：开发流程定义与策略选择

**会议目标**  
定义开发流程和策略，计划“怎么做”。确立技术实现路径、端到端5阶段流程，并将非功能需求嵌入流程中。

**会议前置**  
- 会议一的业务目标和会议二的角色分配已明确。  
- 项目组长准备了现有脚本基线分析（差距表）。

---

#### 1. 核心讨论问题与回答

**问题1：面对现有脚本基线和2周上线压力，应采取什么开发策略？**  
- 团队提出“渐进式服务化”策略：  
  - **短期（3天冲刺）**：将现有Shell/Python脚本封装为标准Python模块+CLI；使用轻量级Web框架（FastAPI）提供REST入口；任务队列暂用多进程池+文件锁，避免引入额外中间件。  
  - **中期（2周上线）**：增加持久化任务队列（Redis+RQ），实现评测任务的异步化与断点续跑；为每个评测生成唯一Run ID，完善审计日志。  
  - **长期**：支持多后端动态调度、容器化Worker水平扩展，满足NFR-05和NFR-12。  
- 管理层认可此策略，认为既务实又具可演进性。

**问题2：技术栈如何选型？**  
- 团队基于需求约束和现有生态逐一讨论：  

| 层级     | 技术选型                                        | 理由                                                         |
| :------- | :---------------------------------------------- | :----------------------------------------------------------- |
| 后端语言 | Python 3.10+                                    | 现有脚本（`results_evaluator`, `mindspeed_fim_bridge`）均为Python；华为云SDK、昇腾CANN Python接口成熟。 |
| Web框架  | FastAPI                                         | 自动生成API文档、异步支持、性能优于Flask，便于后续接入任务队列；3天内可快速搭建。 |
| 任务队列 | 初期`multiprocessing.Pool`+文件锁；中期Redis+RQ | 减少依赖，降低3天冲刺复杂度；设计上预留队列接口，展示可演进性。 |
| 容器化   | Docker（基于Ubuntu 20.04 + Ascend CANN Base）   | 必须符合华为云Model Arts容器规范；运维经理负责多阶段构建。   |
| 前端     | 纯HTML + Tailwind CSS + JavaScript（Fetch API） | 轻量级，无需前端框架；实时日志采用轮询（2秒间隔），首期满足FR-11。 |
| 存储     | 本地文件系统（评测期间）+ OBS（归档）           | 评测中间结果存于本地临时目录，完成后压缩上传至OBS的`artifacts/`前缀。 |
| 版本控制 | Git（私有仓库）                                 | 分支策略：`main`稳定，`develop`集成，`feature/*`每人特性分支。 |

**问题3：如何将学生提交到榜单发布的黑盒流程标准化为可执行的开发流程？**  
- 团队定义了5个标准阶段，每个阶段明确输入、输出、责任人、质量门禁：  

| 阶段  | 流程名称 | 执行角色 | 输入                                            | 输出                                                         | 质量门禁 / 关键动作                                          |
| :---- | :------- | :------- | :---------------------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| Step1 | 提交同步 | 数据经理 | 助教配置：课程ID、作业ID、学生名单、日期范围    | 本地目录 `submissions/{student_id}/` 包含HF权重文件          | ① 使用`ta-client`申请只读STS，临时凭证有效期≤1小时；② 校验每个学生目录根部必须有`config.json`、`tokenizer.json`和至少一个权重文件；③ 校验失败的学生写入`manifest.json`，状态标记为`absent`。 |
| Step2 | 任务编排 | 项目组长 | 助教通过Web界面选择的测试集、后端类型、采样参数 | 任务队列中的`Job`对象（含task_id，学生列表，配置快照）       | ① 为每个学生生成唯一`task_id`（格式：`eval_{course}_{date}_{student_id}_{uuid}`）；② 将评测配置序列化为JSON，保存至`artifacts/{task_id}/config.json`；③ 检查是否已存在相同配置的完成结果，避免重复评测（幂等性）。 |
| Step3 | 评测执行 | 后端经理 | 从队列中拉取Job；本地已下载的学生权重目录       | `generation.jsonl`、`progress.json`、`generate.log`          | ① 调用后端适配器（优先MindSpeed），传入prompt JSONL和模型路径；② 实时捕获stdout/stderr，追加写入`generate.log`；③ 每完成N个样本（N可配置，如10），更新`progress.json`（已完成/总数，ETA）；④ 超时保护：单个样本生成超过30秒或总时长超过2小时，标记该学生为`TLE`并停止。 |
| Step4 | 指标计算 | 后端经理 | `generation.jsonl`、原始参考答案文件            | `results.json`、`repetition_report.json`、`aggregate_report.csv` | ① 复用现有`results_evaluator`和复读分析脚本，不重写算法；② 计算EM、ES、ES_repoeval、tle_ratio、复读率；③ 按照需求文档公式计算`Score`（缺失权重归一化）；④ 写入`results.json`并压缩上传至OBS `artifacts/{task_id}/`。 |
| Step5 | 榜单发布 | 运维经理 | `aggregate_report.csv`（来自所有学生）          | 公开HTML榜单 + 内部详细报告                                  | ① 使用Jinja2模板生成两个版本：`leaderboard_public.html`（仅排名、Score、ES、TLE通过率、非复读率、状态）和`leaderboard_internal.html`（含绝对路径、逐样本详情）；② 脱敏过滤：用正则移除所有`/home/|/root/|C:\|model_path`等敏感字符串；③ 将公开HTML上传到OBS `leaderboard/`前缀并设置公共读权限；④ 助教确认后通知学生访问。 |

**问题4：如何将非功能需求（安全性、可复现性、可观测性等）嵌入流程？**  
- 团队在每个阶段设置了检查点：  
  - **安全性**：`ta-client`中从环境变量获取角色ARN，调用外部`issuer-server`获取临时凭证；代码仓库配置pre-commit hook扫描硬编码密钥（CI）。  
  - **可复现性**：每个`task_id`目录下保存：评测脚本的Git commit ID、数据集文件（或hash）、后端版本（`pip freeze`）、配置快照、原始generation.jsonl。  
  - **可观测性**：每个请求分配`trace_id`；`progress.json`每5秒刷新；Web页面通过轮询展示最新日志尾部（类似`tail -f`）。  
  - **可靠性**：Step3超时后标记`TLE`；Step4计算失败时保留`generation.jsonl`并标记`recompute_failed`；支持通过CLI `--resume`从断点继续。  
  - **公平性**：助教创建任务时锁定测试集版本（如`fim_dataset_v1.0`），后端参数（temperature=0.2, top_p=0.95）写入全局配置，学生不可更改。

**问题5：3天内如何分步实施？**  
- 制定每日冲刺计划：  
  - **第1天**：项目组长搭建Git仓库和基本目录结构；运维经理完成Dockerfile和基础镜像配置；后端经理提取`evaluator`为独立包；数据经理实现`ta-client`框架。产出：可运行的FastAPI空服务、OBS mock下载。  
  - **第2天**：后端经理封装`run_evaluator.py`（支持mock模式）；数据经理实现真实OBS下载+校验；运维经理完成Web静态页面+轮询逻辑；项目组长实现任务编排API。产出：通过CLI可触发mock评测，前端能看到日志。  
  - **第3天**：集成所有模块，在华为云Notebook上跑通MindSpeed真实评测；生成脱敏榜单；撰写方案文档并录制演示视频。产出：端到端可演示原型+完整方案PDF。

---

#### 2. 会议输出

- 正式采纳“渐进式服务化”策略。  
- 技术栈锁定：FastAPI + Python + Docker + 多进程队列（首期），后续可平滑升级到Redis RQ。  
- 5阶段开发流程定义完成，作为后续编码和评审的依据。  
- 每日冲刺计划（3天）写入整体计划。  
- 待办：项目组长会后12小时内提交Git仓库初始版本；后端经理提供`requirements.txt`；数据经理研究华为云STS API；运维经理准备Docker基础镜像。

---

### 会议四：整体计划

**会议目标**  
制定项目整体的里程碑和进度计划，确立时间框架。聚焦3天详细计划，确保每人知道每小时的交付任务，并预留缓冲。

**会议前置**  
- 会议三的5阶段流程和冲刺计划已初步确定，需要细化为可执行的时间表。  
- 项目组长准备了工作分解结构（WBS）草案。

---

#### 1. 核心讨论问题与回答

**问题1：如何将3天冲刺计划分解为精确到小时的任务？**  
- 团队采用“自顶向下+关键路径”方法：  
  - 先确定三个里程碑（M1、M2、M3）的截止时间点。  
  - 再按角色分解每日任务，每个任务标明起止时间、产出物、依赖项。  
  - 示例（第1天部分任务）：  

| 时间        | 任务                   | 责任人   | 产出物                      | 依赖                   |
| :---------- | :--------------------- | :------- | :-------------------------- | :--------------------- |
| 08:00-09:00 | 站立会                 | 项目组长 | 更新看板                    | 无                     |
| 09:00-11:00 | 编写API契约（OpenAPI） | 项目组长 | `openapi.yaml`              | 无                     |
| 09:00-11:00 | 编写`sts_client.py`    | 数据经理 | `sts_client.py`             | 需助教提供STS endpoint |
| 11:00-12:00 | 设计元数据Schema       | 项目组长 | `task_metadata_schema.json` | 无                     |
| ...         | ...                    | ...      | ...                         | ...                    |

（完整表格略，实际方案中附有3天共72小时级的任务分解）

**问题2：关键路径是什么？哪些任务必须严格按时？**  
- 识别出关键路径：`OBS拉取校验（数据经理，第1天）→ 任务编排（项目组长，第2天上午）→ 评测执行封装（后端经理，第2天全天）→ 指标计算（后端经理，第2天下午）→ 榜单发布（运维经理，第3天上午）`。  
- 最长链总工作量约16小时，可在2个日历日内完成。  
- 关键路径任务偏差容忍度为0，一旦延迟需立即动用缓冲资源。

**问题3：如何估算工作量并保证计划可行？**  
- 使用历史效率数据（基于过往类似项目）：  
  - 项目组长：架构设计/文档编写 4人时/复杂设计，2人时/简单接口。  
  - 后端经理：Python模块封装 2人时/模块（约200行）。  
  - 数据经理：新接口对接 3人时/接口（含学习时间）。  
  - 运维经理：Docker+前端 4人时/完整页面。  
- 有效工作时间按每日14小时（08:00-22:00）的60%-70%计算，即每人每日可用约9-10人时。  
- 计划总核心任务人时约47.5，团队3天可用人时84，缓冲比例43%，远高于TSP建议的20%-30%。

**问题4：如何应对高风险任务（如华为云环境、MindSpeed兼容）？**  
- 为每个高风险任务设置缓冲时间：  
  - OBS STS权限申请：第1天预留2小时（20:00-22:00）处理突发。  
  - MindSpeed环境配置：第2天预留2小时（20:00-22:00）调试。  
  - 集成测试：第2天预留2小时，第3天预留2小时。  
- 若缓冲耗尽仍失败，触发降级方案（使用mock数据继续演示，标注已知限制）。

**问题5：资源分配是否平衡？是否存在单人过载？**  
- 计算每位成员每日人时：  
  - 第1天：每人约11人时（含会议、协作）。  
  - 第2天：每人约11人时。  
  - 第3天：项目组长9人时，其他成员8人时。  
- 差异在合理范围内（≤30%）。若某成员提前完成，主动支援他人；若严重滞后，项目组长重新分配任务。

---

#### 2. 会议输出

- 3天冲刺详细计划表（小时级，包含所有成员任务）。  
- 关键路径图与依赖清单。  
- 缓冲分配方案。  
- 全体成员确认计划，并承诺按计划执行。  
- 待办：项目组长将计划写入项目看板；数据经理准备mock OBS目录；后端经理准备MindSpeed环境测试脚本。

---

### 会议五：质量计划

**会议目标**  
设定质量目标，并制定达成目标的详细计划。聚焦安全、可复现性、进度透明度三条红线，采用轻量级质量活动，避免过度流程消耗。

**会议前置**  
- 会议四的整体计划已确定，需要嵌入质量检查点。  
- 质量经理（由数据经理兼任）准备了初步的质量风险清单。

---

#### 1. 核心讨论问题与回答

**问题1：哪些质量维度是必须守住的“红线”？如何量化？**  
- 团队基于NFR和专家关注点，确定三个必须达成的质量目标：  

| 质量维度   | 对应NFR | 量化阈值                                                     | 验证方法                                                     |
| :--------- | :------ | :----------------------------------------------------------- | :----------------------------------------------------------- |
| 安全性     | NFR-01  | 代码仓库中硬编码AK/SK的缺陷数 = 0                            | CI流水线使用`detect-secrets`或`git-secrets`扫描，每次push自动拦截。 |
| 可复现性   | NFR-09  | 每次评测结果的Run ID必须关联Git Commit ID + 数据集版本哈希，缺失率 = 0 | 人工抽查2个评测任务，检查`artifacts/{task_id}/metadata.json`是否包含完整字段。 |
| 进度透明度 | NFR-07  | 进度追踪数据与实际完成情况的偏差 ≤ 10%（每10分钟刷新）       | 模拟评测任务，对比日志输出时间戳与进度文件内容，偏差超过10%视为不合格。 |

- 次要质量目标（允许有缺陷，但需记录）：前端UI样式异常、非核心日志冗余、指标计算浮点误差≤0.01%。

**问题2：如何控制缺陷注入与消除？**  
- 设定各阶段目标：  
  - 编码阶段：核心模块（`ta-client`、`task_runner`、`leaderboard/filter.py`）缺陷密度 ≤3个/千行。  
  - 集成测试阶段：消除≥80%缺陷，其中安全/可复现性/进度缺陷必须100%消除。  
  - 方案提交前：三大质量目标缺陷清零。  
- 预防措施：  
  - 安全：pre-commit hook扫描硬编码。  
  - 可复现性：`task_runner`启动时自动获取`git rev-parse HEAD`和数据集md5。  
  - 进度：评测每完成10%样本自动写入时间戳和计数。

**问题3：在3天冲刺中，质量活动应投入多少时间？采用哪些具体活动？**  
- 质量活动总时间不超过开发总时间的15%（约10.8人时）。  
- 具体轻量级活动：  

| 活动         | 频率                    | 参与人            | 时长      | 产出                              |
| :----------- | :---------------------- | :---------------- | :-------- | :-------------------------------- |
| 安全代码评审 | 仅一次（第2天10:00）    | 数据经理+项目组长 | 30分钟    | 评审报告，确认无AK/SK硬编码       |
| 可复现性抽查 | 每日一次（17:00站立会） | 后端经理          | 10分钟/次 | 抽查一个已完成任务的metadata.json |
| CI自动化扫描 | 每次push到`develop`     | 运维经理配置      | 5分钟     | 扫描报告，阻断含硬编码的提交      |
| 进度偏差验证 | 第2天15:00手动测试      | 运维经理          | 15分钟    | 对比前端显示与日志实际完成数      |

**问题4：如何快速响应质量风险？**  
- 设立1小时熔断机制：  

| 风险场景                 | 触发条件                              | 应对措施                                         | 最大修复时间 |
| :----------------------- | :------------------------------------ | :----------------------------------------------- | :----------- |
| 硬编码AK/SK被检测到      | CI扫描失败或代码评审发现              | 立即删除硬编码，改用环境变量注入；轮换已泄露凭证 | 30分钟       |
| Run ID无法关联Git Commit | 抽查发现metadata.json中commit字段为空 | 修复脚本强制获取；对已产生任务手工补录           | 1小时        |
| 进度轮询卡死或严重偏差   | 前端显示30%但日志显示100%             | 检查文件写入权限；降级为“仅显示状态”             | 1小时        |

- 若1小时内无法修复：  
  - 安全问题：立即停止代码提交，全员集中解决，否则方案不可提交。  
  - 其他问题：在方案中标注为已知限制，承诺2周上线前修复。

**问题5：质量承诺如何写入方案以打动专家？**  
- 团队拟定以下承诺语句：  
  > “安全性承诺：系统内所有与华为云OBS/STS的交互均使用临时凭证，长期密钥绝不出现在代码或配置中。CI流水线会强制扫描并阻止任何硬编码提交。  
  > 可复现性承诺：每次评测任务生成全局唯一的Run ID，并关联Git Commit ID、数据集版本哈希、后端版本及采样参数。任何榜单结果均可回溯至原始配置和代码。  
  > 进度透明度承诺：评测过程中，前端每2秒轮询进度文件，偏差不超过10%。学生和助教可实时看到任务执行状态和预估剩余时间。  
  > 缺陷控制承诺：在3天冲刺期间，核心模块的缺陷密度控制在3个/千行以内，且上述三大质量目标的缺陷在提交方案前全部清零。”

---

#### 2. 会议输出

- 三大质量红线及其量化阈值正式确认。  
- 轻量级质量活动日程已加入整体计划。  
- 熔断机制和降级方案建立。  
- 待办：数据经理今天10:00前完成CI安全扫描配置；运维经理准备进度验证Mock测试；项目组长将质量承诺模板写入方案文档。

---

### 会议六：个人计划以及计划平衡

**会议目标**  
成员制定个人计划，协调平衡工作量。将团队整体计划分解为个人可执行任务，并建立动态再平衡机制。

**会议前置**  
- 会议四的整体计划（小时级）需要进一步细化到每人每日的具体任务。  
- 每个成员已根据自身能力草拟了个人任务清单。

---

#### 1. 核心讨论问题与回答

**问题1：如何确保个人任务既具体又可验证？**  
- 规定每个任务必须满足：  
  - 有明确的开始和结束时间（精确到小时）。  
  - 有可检验的产出物（如“提交`openapi.yaml`文件”而非“完成API设计”）。  
  - 标注依赖项（如“依赖数据经理提供OBS目录结构规范”）。  
- 示例错误：❌“完成数据拉取模块”  
- 示例正确：✅“6月3日14:00前提交`ta-client`模拟脚本，输出符合`submissions/{run_id}/`格式的目录结构”。

**问题2：每个人的历史效率数据如何影响任务分配？**  
- 团队依据过往课程项目或实验数据，给出参考值：  
  - 项目组长：架构设计4人时/复杂模块，文档编写2人时/千字。  
  - 后端经理：Python模块封装2人时/模块，调试1人时/百行。  
  - 数据经理：新SDK对接3人时/接口（含学习），熟悉接口1.5人时/接口。  
  - 运维经理：Dockerfile编写2人时，前端页面4人时/完整页面。  
- 按此效率分配任务量，确保每人每日有效工作负荷在9-10人时。

**问题3：如果某个成员任务过重或过轻，如何动态调整？**  
- 每日17:00站立会检查实际耗时 vs 计划耗时。  
- 调整规则：  
  - 若某成员实际耗时超出计划20%，且连续两天发生，则从负荷最轻的成员转移部分任务。  
  - 若关键路径任务延迟超过2小时，立即启动全员支援模式：暂停非关键任务，所有人协助解决。  
  - 若某成员提前完成，主动认领队列中其他未完成的高优先级任务（如协助测试、文档编写）。  
- 缓冲时间（20:00-22:00）优先用于追赶滞后任务。

**问题4：如何显式标注依赖关系以避免等待？**  
- 每个任务在计划表中增加“依赖”列，例如：  
  - “后端经理的`task_runner.py`依赖数据经理6月3日18:00前提供的`manifest.json`格式”。  
  - “运维经理的前端轮询依赖项目组长6月4日15:00前完成的进度API”。  
- 依赖项必须在每日站立会上确认状态；若上游延迟，下游立即调整计划并通知项目组长。

**问题5：个人承诺如何体现？**  
- 每位成员在会议上口头承诺交付时间与质量标准，并由项目组长记录。  
- 示例承诺：  
  - “我（后端经理）承诺在6月4日12:00前提供`run_evaluator.py`（支持mock模式），并配合集成测试。”  
- 任何违反承诺的行为需在站立会上说明原因，并给出补救计划。

---

#### 2. 会议输出

- 3天个人计划表（含每日任务、时间、产出、依赖）。  
- 工作量平衡分析报告（确认每人每日负荷在合理范围）。  
- 动态再平衡规则（含全员支援模式触发条件）。  
- 全体成员签字确认个人计划。  
- 待办：项目组长汇总个人计划并上传至共享文档；数据经理准备依赖关系矩阵。

---

## 第三阶段：风险控制

### 会议七：风险评估与项目启动批准

**会议目标**  
识别、评估项目的主要风险，制定初步的缓解计划，并向管理层汇报以获取正式批准。用数据证明计划可行性，并设立熔断条件。

**会议前置**  
- 前六次会议已输出完整的产品目标、角色、流程、计划、质量、个人任务。  
- 项目组长准备了风险登记册和汇报材料。  
- 管理层代表（助教、课程负责人）受邀参加。

---

#### 1. 核心讨论问题与回答（管理层质询环节）

**问题1（管理层）：如何证明3天提交方案、2周上线的计划是现实的？**  
- 项目组长展示历史效率数据与任务量对比表：  

| 角色     | 本次核心任务量                          | 历史平均效率               | 计划耗时     | 偏差分析                                                 |
| :------- | :-------------------------------------- | :------------------------- | :----------- | :------------------------------------------------------- |
| 项目组长 | API设计+集成+文档（约4个模块）          | 4.2人时/模块               | 4.0人时/模块 | 复用现有OpenAPI模板，效率提升5%                          |
| 后端经理 | 评测脚本封装（4个模块）+Mock生成（2个） | 2.1人时/模块（σ=0.3）      | 2.0人时/模块 | 历史数据含学习成本，本次直接复用`evaluator`包，保守取2.0 |
| 数据经理 | OBS拉取+校验（3个模块）                 | 3.5人时/模块（含首次对接） | 3.0人时/模块 | 已有`course-obs-sts`参考代码，节省0.5h/模块              |
| 运维经理 | Docker+前端+轮询（3个模块）             | 4.0人时/模块               | 3.5人时/模块 | 使用Tailwind模板，减少UI调试时间                         |

- 总核心人时47.5，团队3天可用人时84，缓冲43%。关键路径最长16小时，2天内可完成。管理层认可。

**问题2（管理层）：关键资源（OBS凭证、MindSpeed环境）是否存在单点故障？**  
- 数据经理回应：OBS STS凭证若延迟，已准备本地Mock目录（含3个示例提交），不阻塞其他模块。第2天若仍未拿到凭证，由运维经理协助在华为云控制台手动生成临时AK/SK（仅内部测试）。  
- 后端经理回应：MindSpeed环境由两人共同负责（后端+运维），若CANN版本冲突，则使用Docker容器封装固定环境（Dockerfile已写好），最晚第2天12:00可切换。  
- 管理层认可资源风险可控。

**问题3（管理层）：最重要的三个风险是什么？熔断机制是否具体可执行？**  
- 项目组长展示风险-预案映射表：  

| 风险                            | 触发条件（精确时间）       | 熔断动作（责任人+时间）                                      | 对方案提交的影响                                             |
| :------------------------------ | :------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| OBS STS凭证未下发               | 第1天18:00仍无有效凭证     | 数据经理立即切换为本地Mock目录，每2小时重试申请；向管理层报备。 | 0延迟（Mock支持全流程）                                      |
| MindSpeed真实环境无法生成       | 第2天15:00仍无法跑通单样本 | 后端经理降级为Mock生成器；运维经理协助在方案中标注“真实后端待环境稳定后集成”。 | 演示仍可用Mock数据，不影响方案评审；承诺2周上线前修复。      |
| 端到端集成失败（API→评测→榜单） | 第2天17:00核心流程卡死     | 项目组长组织全员集中调试2小时；若仍未解决，则录制部分成功片段+说明文档替代完整演示。 | 方案中可展示“已实现模块”，承认集成风险，承诺第3天22:00前修复。 |

- 管理层认为预案具体、可执行，同意批准。

**问题4（管理层）：批准需要附带哪些约束条件？**  
- 管理层提出以下条件：  
  - **进度锚点**：第1天18:00必须完成OBS拉取Mock验证，否则自动触发熔断并发送风险报告。  
  - **关键依赖交付**：第2天12:00前，后端经理必须提供`task_runner.py`（至少Mock模式）并可被集成。若未完成，项目组长有权重新分配任务。  
  - **透明度**：每日17:00站立会需向管理层同步计划vs实际耗时、阻塞问题、熔断状态。  
  - **变更冻结**：批准后禁止新增功能需求，仅允许调整实现方式（如从多进程改为文件锁）。  
- 团队全部接受。

**问题5（管理层）：需要管理层提供哪些支持？**  
- 团队提出资源请求：  
  - 今日18:00前提供OBS只读临时凭证（有效期至少7天）。  
  - 保留专属资源池Notebook（`NB_TA`）的独占访问权至6月19日。  
  - 若遇到华为云平台级故障，同意方案提交deadline顺延24小时。  
- 管理层全部承诺。

---

#### 2. 会议输出

- 管理层正式有条件批准项目启动，批准条件记录在案。  
- 风险登记册（含概率、影响、缓解措施、责任人）最终定稿。  
- 熔断机制及触发条件获得授权。  
- 待办：  
  - 项目组长将批准条件写入`docs/approval.md`并更新看板。  
  - 数据经理在16:00前完成Mock OBS目录准备。  
  - 后端经理今晚20:00前在Notebook上跑通MindSpeed单条生成并录屏。  
  - 运维经理准备每日进度报告模板（含计划vs实际耗时）。  
- 全体成员签署项目启动承诺书。

---